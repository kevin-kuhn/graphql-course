# Comandos

```
	npm init -y
	npm install apollo-server graphql
	npx https://github.com/luizomf/eslint-prettier
  npm install -D sucrase -> para poder rodar arquivos em module (sucrase/register)
  npm install -D nodemon -> fica olhando nosso projeto pra quando ouver alguma alteração ele reinicia ele automaticamente,
                            pra não ter que ficar reiniciando o codigo o tempo todo

```
